---
description: Checkpoint current work — update backlog, changelog, commit, and suggest next item
---

Invoke the `super-agent-skills:wrap-up` skill.

Update tracking artifacts (backlog, changelog), commit the current work, and suggest the next item from the backlog.

Use argument as description of what was completed if provided: $ARGUMENTS
